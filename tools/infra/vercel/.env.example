# Vercel API token (create at https://vercel.com/account/settings/tokens).
#
# In a Centaur sandbox, leave this unset: the wrapper falls back to the
# VERCEL_TOKEN placeholder and iron-proxy injects the real credential on
# requests to api.vercel.com. Set a real value only for standalone local use
# outside iron-proxy.
VERCEL_TOKEN=
