[project]
name = "figma"
description = "Figma design system extraction and analysis"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "typer>=0.12.0",
    "rich>=13.0.0",
    "python-dotenv>=1.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"


[tool.centaur]
module = "client.py"
secrets = [
    {type = "http", name = "FIGMA_ACCESS_TOKEN", match_headers = ["X-Figma-Token"], hosts = ["api.figma.com"]},
]
