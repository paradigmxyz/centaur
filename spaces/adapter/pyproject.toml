[project]
name = "spaces-adapter"
version = "0.1.0"
description = "Thin Centaur session API client for the Spaces product layer"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/spaces_adapter"]
